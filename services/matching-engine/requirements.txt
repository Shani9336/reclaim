# Core
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pydantic==2.9.2

# ML — Sentence Transformers
sentence-transformers==3.1.1
torch==2.4.1          # CPU version — remove +cpu suffix for GPU
numpy==1.26.4

# Optional: CLIP image matching (Phase 2 — not active in MVP)
# Pillow==10.4.0
# open-clip-torch==2.26.1

# Testing
pytest==8.3.3
httpx==0.27.2
